% layout 'default';
% title 'Browse';

<ul>
    % for my $doc ( @$documents ) {
    <li>
        <a href="/document/<%= $doc->{sha1} %>">
            <%= $doc->{filename} %>
        </a>
        <%= $doc->{size} %>
    </li>
    % }
</ul>
